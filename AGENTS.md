# Revo Run Agent Contract

Use the workspace canonical agent playbook. This package is strict TypeScript ESM. Preserve the root-only public surface and DBOS continuation authority. Run `pnpm verify`; database integration requires `DATABASE_URL` and matching `REVO_RUN_TEST_DATABASE=revo_run_test_*`.
