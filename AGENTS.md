# Revo Run Agent Contract

Use the workspace canonical agent playbook. This package is strict TypeScript ESM. Preserve the
root-only public surface and do not restore removed `0.1.x` compatibility code. Run `pnpm verify`.
