export const runWorkflowId = (runId: string): string => `rr:run:${runId}`;

export const scopeWorkflowId = (scopeId: string): string => `rr:scope:${scopeId}`;

export const commandWorkflowId = (commandId: string): string => `rr:command:${commandId}`;
