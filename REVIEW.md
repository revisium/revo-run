# Review Contract

Block changes that expand the root runtime/type surface, add deep exports, make host snapshots continuation authority, expose DBOS naming/configuration, transform or impose format assumptions on caller-supplied opaque top-level run IDs, accept ambiguous or caller-controlled child/execution IDs, weaken isolated database safety, or omit public package proof.
